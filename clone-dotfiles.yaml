---
- hosts: all
  name: Clone dotfiles

  vars_files:
    - vars/paths.yaml

  vars:
    homeshick:
      repos:
        - host:
            gitlab
          castle:
            base
        - host:
            gitlab
          castle:
            basic
        - host:
            github
          castle:
            homebin

  roles:
    - homeshick
