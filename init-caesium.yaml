---
- include:
    init-workstation.yaml
  tags:
    workstation

- include:
    install-stumpwm.yaml
  tags:
    stumpwm

- hosts: all
  name: Initialize caesium

  vars:
    deps:
      apt:
        - chromium-browser
        - network-manager

  roles:
    - { role: packages, tags: host-specific }
