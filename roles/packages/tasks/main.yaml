---
- name: Install apt specific requirements for {{_name}}
  apt: name={{item}} state=latest update_cache={{update_cache | default(False)}}
  with_items:
    "{{_apt | default([])}}"
  when: ansible_os_family == "Debian"
  become: yes
  tags: reqs, apt-reqs
- name: Install dnf specific requirements for {{_name}}
  dnf: name={{item}} state=latest
  with_items:
    "{{_dnf_yum | default([])}}"
  when: ansible_distribution == "Fedora"
  become: yes
  tags: reqs, reqs-dnf
- name: Install portage specific requirements for {{_name}}
  portage: name={{item}} state=present
  with_items:
    "{{_portage | default([])}}"
  when: ansible_distribution == "Gentoo"
  become: yes
  tags: reqs
- name: Install yum specific requirements for {{_name}}
  yum: name={{item}} state=latest
  with_items:
    "{{_dnf_yum | default([])}}"
  when: ansible_os_family == "RedHat" and ansible_distribution != "Fedora"
  become: yes
  tags: reqs, reqs-yum
