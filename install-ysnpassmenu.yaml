---
- hosts: all
  name: Install ysnpassmenu

  vars_files:
    - vars/paths.yaml

  vars:
    repo:
      name: ysnpassmenu
      url: "https://github.com/femnad/ysnpassmenu.git"
    util:
      link: ysnpassmenu
      link_directory: "{{ user_bin }}"

  roles:
    - clone
    - util

- include: install-dmenu.yaml
  tags: dmenu

- include: install-xdotool.yaml
  tags: xdotool
