---
- hosts: all
  vars_files:
    - paths.yaml
  vars:
    - project: slock
    - repo: git://git.suckless.org/slock
    - apt_packages: libxrandr-dev
  roles:
    - os_packages
    - git_clone
    - autotools_project
